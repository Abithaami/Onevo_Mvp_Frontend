export default function PlaceholderPage({ title, description }) {
  return (
    <section className="placeholder-page">
      <h1>{title}</h1>
      <p>{description}</p>
      <small>UI scaffold ready for implementation.</small>
    </section>
  );
}
